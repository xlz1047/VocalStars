# Shared Resources

The `shared/` folder is reserved for cross-project contracts, reusable types, and shared utilities between frontend and backend.

## Example

- `types.ts` — shared TypeScript interfaces for future API response schemas and feature models.
